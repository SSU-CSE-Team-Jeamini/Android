<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:background="@drawable/ranking_item_bg_white"
    android:padding="10dp"
    android:layout_marginBottom="8dp">

    <!-- 순위 원형 (동적으로 색상 변경) -->
    <LinearLayout
        android:id="@+id/layout_rank_circle"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:gravity="center"
        android:background="@drawable/rank_circle_bg"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/tv_rank"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="4"
            android:textColor="#000000"
            android:textSize="15sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- 이름 -->
    <TextView
        android:id="@+id/tv_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="사용자"
        android:textColor="#111111"
        android:textStyle="bold"
        android:textSize="15sp"
        android:layout_marginStart="12dp"
        app:layout_constraintStart_toEndOf="@id/layout_rank_circle"
        app:layout_constraintTop_toTopOf="@id/layout_rank_circle"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 칼로리 -->
    <TextView
        android:id="@+id/tv_kcal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔥 6520 kcal"
        android:textColor="#555555"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="@id/tv_name"
        app:layout_constraintTop_toBottomOf="@id/tv_name"
        android:layout_marginTop="3dp" />

    <!-- 펀치 횟수 -->
    <TextView
        android:id="@+id/tv_punch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🥊 8900회"
        android:textColor="#555555"
        android:textSize="12sp"
        app:layout_constraintStart_toEndOf="@id/tv_kcal"
        app:layout_constraintTop_toTopOf="@id/tv_kcal"
        android:layout_marginStart="10dp" />

</androidx.constraintlayout.widget.ConstraintLayout>